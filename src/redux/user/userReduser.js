const initialStore = {
    user: null
}

const userReduser = (stote = initialStore, action) => {
    switch(action.type) {
        default:
            return stote;
    }
}

export default userReduser;